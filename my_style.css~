.wrapper {
  max-width: 2560px; /* 20px smaller, to fit the paddings on the sides */
}
.topnav
{
	background-color: white;
	font-family: Athiti;
  padding: 10px;
  text-align: left;
  color: black;
  font-size: 80px;
}
.header 
{
	font-family: Athiti;
  padding: 10px;
  text-align: left;;
  background: white;
  color: black;
  font-size: 80px;
}
@font-face {
  font-family: Athiti;
  src: url(Athiti/Athiti-Bold.ttf);
  font-weight: bold;
}
@font-face {
  font-family: Athiti;
  src: url(Athiti/Athiti-SemiBold.ttf);
  font-weight: semibold;
}
@font-face {
  font-family: Athiti;
  src: url(Athiti/Athiti-Medium.ttf);
  font-weight: medium;
}
@font-face {
  font-family: Athiti;
  src: url(Athiti/Athiti-Regular.ttf);
  font-weight: regular;
}
@font-face {
  font-family: Athiti;
  src: url(Athiti/Athiti-Light.ttf);
  font-weight: light;
}
@font-face {
  font-family: Athiti;
  src: url(Athiti/Athiti-ExtraLight.ttf);
  font-weight: extralight;
}

* {box-sizing: border-box}

/* Container needed to position the overlay. Adjust the width as needed */
.container {
  position: relative;
  width: auto;
  max-width: 2560px;
}

/* Make the image to responsive */
.image {
  display: block;
  width: 100%;
  height: auto;
}

/* The overlay effect - lays on top of the container and over the image */
.overlay {
  position: absolute;
  right: 50%;
  top: 0;  
  bottom: 0;
  background: rgba(0, 0, 0, 0.5); /* Black see-through */
  color: #f1f1f1;
  width: 50%;
  transition: .5s ease;
  opacity:0;
  color: white;
  font-size: 60px;
  padding-top: 25%;
  text-align: center;
}

/* When you mouse over the container, fade in the overlay title */
.container:hover .overlay {
  opacity: 1;
}